diff --git a/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java b/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
index 025480a..f0d3b2d 100644
--- a/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
+++ b/node_modules/react-native-callkeep/android/src/main/java/io/wazo/callkeep/RNCallKeepModule.java
@@ -434,11 +434,16 @@ public class RNCallKeepModule extends ReactContextBaseJavaModule implements Life
         this.hasListeners = false;
     }
 
-    @ReactMethod
     public void displayIncomingCall(String uuid, String number, String callerName) {
         this.displayIncomingCall(uuid, number, callerName, false, null);
     }
 
+    // Only one overload of a method may be exposed to JS as @ReactMethod: the
+    // New Architecture's TurboModule interop layer (unlike the legacy bridge)
+    // requires unique JS-facing method names and crashes the whole process
+    // with SIGABRT the first time JS touches this module otherwise. The
+    // Android JS wrapper (index.js) only ever calls this 4-arg overload, so
+    // it's the one kept reachable from JS.
     @ReactMethod
     public void displayIncomingCall(String uuid, String number, String callerName, boolean hasVideo) {
         this.displayIncomingCall(uuid, number, callerName, hasVideo, null);
@@ -483,11 +488,13 @@ public class RNCallKeepModule extends ReactContextBaseJavaModule implements Life
         conn.onAnswer();
     }
 
-    @ReactMethod
     public void startCall(String uuid, String number, String callerName) {
         this.startCall(uuid, number, callerName, false, null);
     }
 
+    // See the comment on displayIncomingCall above: only one overload per
+    // JS-facing name is allowed under the New Architecture. index.js only
+    // ever calls this 4-arg overload on Android.
     @ReactMethod
     public void startCall(String uuid, String number, String callerName, boolean hasVideo) {
         this.startCall(uuid, number, callerName, hasVideo, null);
