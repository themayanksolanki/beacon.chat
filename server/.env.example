PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
# Postgres connection string (Neon/Supabase/RDS/local). Prisma reads this
# directly — see prisma/schema.prisma. Run `npm run prisma:migrate:dev`
# locally / `npm run prisma:migrate` in prod after setting this.
DATABASE_URL=postgresql://user:password@localhost:5432/beacon

# Optional: real email delivery via Brevo (https://brevo.com). If unset,
# OTP codes and invite emails are logged to the console instead (fine for
# local dev, not for production). BREVO_FROM needs a verified sender/domain
# in Brevo before it can send to arbitrary recipients.
BREVO_API_KEY=
BREVO_FROM=no-reply@beaconchat.app
BREVO_FROM_NAME=Beacon

# Optional: link included in "invite a friend" emails.
APP_DOWNLOAD_URL=https://beaconchat.app/download

# Optional: real SMS delivery via MSG91 (https://msg91.com) for phone-number
# OTP login/verification. If any of these three are unset, OTP codes are
# logged to the console instead (fine for local dev, not for production).
# MSG91_TEMPLATE_ID is a DLT-approved Flow template id (India requires DLT
# registration for transactional SMS) — see sms.ts for the exact payload.
MSG91_AUTH_KEY=
MSG91_SENDER_ID=
MSG91_TEMPLATE_ID=

# Optional: AWS S3 for profile-avatar storage (presigned uploads). If unset,
# POST /profile/avatar/upload-url returns 503 and avatar uploads are
# disabled; name-only profile updates still work.
AWS_REGION=
AWS_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# DEV ONLY: set to "true" to expose POST /auth/dev-login, which issues a
# session for any email/publicKey with no OTP check at all — a full
# authentication bypass. Leave unset everywhere except local/throwaway dev
# environments; never set this in production.
SKIP_OTP=